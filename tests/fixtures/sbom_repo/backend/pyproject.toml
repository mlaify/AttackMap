[project]
name = "backend"
version = "0.1.0"
dependencies = [
  "flask>=3.0",
  "requests>=2.28,<3",
  "sqlalchemy[asyncio]>=2.0; python_version >= '3.10'",
]

[project.optional-dependencies]
dev = [
  "pytest>=8.0",
  "ruff",
]
